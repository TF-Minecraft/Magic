# Placeholder gear skins. Fran replaces ia. paths.
# Each scheme maps archetype -> ItemRef path (ia.namespace:id or v.MATERIAL[.cmd]).

iron:
  models:
    - staff(ia.tfmc_magic:iron_staff)
    - wand(ia.tfmc_magic:iron_wand)
    - sword(ia.tfmc_magic:iron_sword)

steel:
  models:
    - staff(ia.tfmc_magic:steel_staff)
    - wand(ia.tfmc_magic:steel_wand)
    - sword(ia.tfmc_magic:steel_sword)

abyssalite:
  models:
    - staff(ia.tfmc_magic:abyssalite_staff)
    - wand(ia.tfmc_magic:abyssalite_wand)
    - sword(ia.tfmc_magic:abyssalite_sword)

mythril:
  models:
    - staff(ia.tfmc_magic:mythril_staff)
    - wand(ia.tfmc_magic:mythril_wand)
    - sword(ia.tfmc_magic:mythril_sword)
